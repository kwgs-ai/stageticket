<% @page_title = "会員の新規登録" %>

<h1><%= @page_title %></h1>

<%= form_for @actor do |form| %>
  <%= render "form", form: form %>
  <div><%= form.submit class: "edit-stage" %></div>
<% end %>
<%= link_to "<<戻る", :back, class: "link_to" %>