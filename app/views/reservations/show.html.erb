<% if @reservation.present? %>
  <table class="list">
    <thead>
    <tr>
      <th>予約番号</th>
      <th>席種</th>
      <th>舞台タイトル</th>
      <th>団体名</th>
      <th>日時</th>
      <th>午前・午後</th>
      <th>本文</th>
    </tr>
    </thead>
    <tbody>
    <tr>
      <td style="text-align: right">
        <%= @reservation.id %>
      </td>
      <td>
        <%= @reservation.stage.title %>
      </td>
      <td>
        <%= @reservation.stage.title %></td>
      <td>
        <%= @reservation.stage.actoraccount.actor_name %>
      </td>
      <td><%= @reservation.stage.date %></td>
      <td><%= @reservation.stage.time %></td>
      <td><%= @reservation.stage.text %></td>
    </tr>
    </tbody>
    </tbody>
  </table>
<% end %>