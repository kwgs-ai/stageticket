<% @page_title = "公演の新規登録" %>
<h1><%= @page_title %></h1>

<%if @errors.present? %>
  <div id="errors">
    <h3>エラーがあります。</h3>
  <%@errors.each do |e| %>
    <li><%=e %></li>
  <%end %>
  </div>
<%end %>


<div class="register-method">
  <h3>新規登録規則</h3>
  <ul>
    <li>全部必須入力です</li>
    <li>タイトルは１文字以上２０文字以内、本文は１０文字以上４００文字以内入力してください</li>
    <li>今日から三日後以降から登録できます</li>
    <li>もうすでに承認された公演がある日時は登録できません</li>
    <li>席の値段は自然数で入力してください</li>
  </ul>
</div>

<%= form_for @form, model: @form, url: confirm_stages_path do |form| %>
  <%= render "form", form: form %>
  <div class="center-ok"><%= form.submit class:"btn btn--orange btn--cubic btn--shadow"%></div>

<% end %>
